# Generated files with secrets (pgext - postgresql password)
defaults/grafana/provisioning/datasources/datasources.yaml
.env
autoenv.zsh

# SSL certificates and private keys
*.pem
*.key
*.p12
*.pfx
*.jks
*.keystore
id_rsa*
id_dsa*
id_ecdsa*
id_ed25519*

# Credentials
credentials.json
credentials.yml
credentials.yaml
service-account*.json

# Claude Code local data (but keep committed, repo-shared skills)
.claude/*
!.claude/skills/

# MkDocs build output
site/

# Emacs backup / autosave / lockfiles
*~
\#*\#
.\#*
