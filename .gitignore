# Generated files with secrets (pgext - postgresql password)
defaults/grafana/provisioning/datasources/datasources.yaml
.env
autoenv.zsh

# SSL certificates and private keys
*.pem
*.key
*.p12
*.pfx
*.jks
*.keystore
id_rsa*
id_dsa*
id_ecdsa*
id_ed25519*

# Credentials
credentials.json
credentials.yml
credentials.yaml
service-account*.json

# Claude Code local data
.claude/

# Local overrides (generated by init-configs, platform-specific).
# Szablon w defaults/ jest trackowany — ignorujemy tylko kopie w roocie repo.
/docker-compose.local_overrides.yml

# Emacs backup / autosave / lockfiles
*~
\#*\#
.\#*
